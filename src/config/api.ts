import { EvolutionClient, ItemClient, PokemonClient } from "pokenode-ts";

export const apiPokemon = new PokemonClient();
export const apiItem = new ItemClient();
export const apiEvolution = new EvolutionClient();
